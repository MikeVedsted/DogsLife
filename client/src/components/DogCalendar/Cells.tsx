const Cells = () => {
  const now = new Date()
  const daysInCurrentMonth = new Date(
    now.getFullYear(),
    now.getMonth() + 1,
    0
  ).getDate()

  return <tbody></tbody>
}

export default Cells
